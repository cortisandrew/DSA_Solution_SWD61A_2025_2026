The ADT is a description of the expected behaviour and operations.

The Vector ADT represents and ordered set of items.
Each item / element has its place. The place is going to be called the Rank. The rank is the number of other elements preceding it. 
(sort of like the index in an array)

Describe the operations and their behaviour